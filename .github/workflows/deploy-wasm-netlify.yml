name: Deploy Blazor WASM to Netlify

on:
  push:
    branches:
      - main
    paths:
      - 'examples/FlowStateBlazorWasm/**'
      - 'examples/SharedNodesLibrary/**'
      - 'src/FlowState/**'
      - '.github/workflows/deploy-wasm-netlify.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'examples/FlowStateBlazorWasm/**'
      - 'examples/SharedNodesLibrary/**'
      - 'src/FlowState/**'
      - '.github/workflows/deploy-wasm-netlify.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore examples/FlowStateBlazorWasm/FlowStateBlazorWasm.csproj

      - name: Build Blazor WASM
        run: dotnet build examples/FlowStateBlazorWasm/FlowStateBlazorWasm.csproj --configuration Release --no-restore

      - name: Publish Blazor WASM
        run: dotnet publish examples/FlowStateBlazorWasm/FlowStateBlazorWasm.csproj --configuration Release --output ./publish --no-build

      - name: Copy Netlify config
        run: cp examples/FlowStateBlazorWasm/netlify.toml ./publish/wwwroot/

      - name: Deploy to Netlify
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: './publish/wwwroot'
          production-branch: main
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: "Deploy from GitHub Actions - ${{ github.event.head_commit.message }}"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        timeout-minutes: 5

